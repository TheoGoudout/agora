<?php
    $signatureCount = $petition->signatureCount ? $petition->signatureCount : 0;

    if ($petition->latestStatus === "En attente d'e-signatures") {
?>
<progress max="500000" value="<?= $signatureCount; ?>"></progress>
<span class="count"><?= sprintf($this->translate("%s e-signatures"), $signatureCount); /* TODO : format */ ?></span>
<?php
    } else {
        // TODO
    }
?>
